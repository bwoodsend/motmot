# These are the oldest versions of each dependency which are known to work.
# Should motmot become incompatible with any of them, simply re-pin to a
# compatible version both here and in the pyproject.toml
cslug==0.1.0
hirola==0.1.0
numpy==1.16.0
numpy-stl==2.12.0
pykdtree==1.2.0
rockhopper==0.1.1
